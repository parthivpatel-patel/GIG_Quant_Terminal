Pre-rebuild prototype (kitchen-sink engines + 9k-line Flask server).

Not part of the QAlpha evaluation surface. Kept on disk only (this folder is gitignored)
so nothing is lost. Do not submit these files with a hiring packet.
